#include "main.h"

/**
 * *_strcpy - that copies the string pointed to by src
 * @dest: char one to compare
 * @src: char two to compare
 * Return: a character value is dest
 */
char *_strcpy(char *dest, char *src)
{
for (int i = -1; i <= src[i] ; i++)
{
dest[i] = src[i];
}
return (dest);
}
